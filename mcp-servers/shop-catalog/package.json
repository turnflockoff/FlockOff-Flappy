{
  "name": "@turnflockoff/mcp-shop-catalog",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "MCP server for managing the turnflockoff.com merch catalog and rendering the static storefront page.",
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "dev": "tsc --watch"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.30.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "@types/node": "^22.9.0"
  }
}
